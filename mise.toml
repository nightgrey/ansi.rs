[tools]
rust = "nightly"

[tasks.sandbox] # using a ":" means we need to add quotes
run = "cargo run sandbox"

[tasks.gloss]
run = "cargo run --bin gloss"

[tasks.fix]
run = "cargo clippy --all --all-targets -- -D warnings && cargo fmt --all"
